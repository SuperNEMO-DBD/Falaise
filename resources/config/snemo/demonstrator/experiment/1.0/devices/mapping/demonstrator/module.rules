# -*- mode: conf-unix -*-
# List of multi-properties (datatools::multi_properties)

#@description Definitions of mapping rules for the demonstrator module
#@key_label  "model"
#@meta_label "policy"


################################################
[model="demonstrator_module" policy="supersede"]

#@description The mapping directives for the "Detector" device
# Note: This has the effect to build the [1000:0] device identifier.
mapping.device.Detector    : string = "[detector]"

#@description The mapping directives for the "Electronics" device
# Note: This has the effect to build the [2000:0] device identifier.
mapping.device.Electronics : string = "[electronics]"

# #@description The mapping directives for the "CMS" device
# # Note: This has the effect to build the [3000:0] device identifier.
# mapping.device.CMS         : string = "[cms]"


# end of module.rules