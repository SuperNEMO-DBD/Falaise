

[name="pipeline" type="dpp::chain_module"]
  #@config Must define "pipeline" as this is the module flreconstruct will use
  # Define the main chain of modules in the pipeline:
  # 1. Remove PTD bank
   modules : string[1] =  \
    "RemovePTD"

[name="RemovePTD" type="dpp::utils_module"]

#@description The processor mode
mode : string = "remove_banks"

#@description The labels of the banks to be removed
mode.remove_banks.labels : string[1] = "PTD"

# #@description The processor mode
# mode : string = "remove_one_typed_bank"

# #@description The label of the bank to be removed
# mode.remove_one_typed_bank.label : string = "PTD"

# #@description The type (serial tag) of the bank to be removed
# mode.remove_one_typed_bank.type : string = "snemo::datamodel::particle_track_data"

# end